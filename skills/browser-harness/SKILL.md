---
name: browser-harness
description: >-
  Controls a live Chrome browser via CDP for web tasks, UI testing, scraping,
  clicking, form filling, and anything that touches a real page with real
  cookies and login state. Use when browsing websites, automating browser
  actions, testing UI, scraping pages, or doing anything in Chrome. Preferred
  over Playwright for tasks requiring the user's auth state. Full instructions
  are always-loaded from @~/Developer/browser-harness/SKILL.md — invoke this
  slash command to explicitly activate browser mode or troubleshoot.
---

Full runtime instructions are already in context via `@~/Developer/browser-harness/SKILL.md`.

If the connection is unclear, run `browser-harness --doctor` and follow the
output. For setup details, read `~/Developer/browser-harness/install.md`.
